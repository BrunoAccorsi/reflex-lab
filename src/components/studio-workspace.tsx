"use client";

import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { ArrowLeft, Braces, Check, Copy, Download, FilePlus2, Plus, RotateCcw, Save, Sparkles, Trash2, Upload, Wrench } from "lucide-react";
import { blankExperiment, experimentDefinitionSchema, experiments, type ExperimentDefinitionV1, type QuestionDefinition } from "@/lib/experiments";
import { loadPresets, makePreset, savePresets, type StoredPreset } from "@/lib/presets";
import { cn } from "@/lib/utils";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";

type EditorTab = "Definition" | "Questions" | "Policy" | "JSON";
const editorTabs: EditorTab[] = ["Definition", "Questions", "Policy", "JSON"];
function clone<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }
function uid(prefix: string) { return `${prefix}-${Date.now().toString(36)}`; }

function validationMessages(error: unknown) {
  if (error && typeof error === "object" && "issues" in error && Array.isArray((error as { issues: unknown[] }).issues)) {
    return (error as { issues: Array<{ path?: Array<string | number>; message?: string }> }).issues.map((issue) => `${issue.path?.length ? issue.path.join(".") : "$"}: ${issue.message ?? "Invalid value"}`);
  }
  return [error instanceof Error ? error.message : "Invalid JSON"];
}

export function StudioWorkspace() {
  const [definition, setDefinition] = useState<ExperimentDefinitionV1>(() => clone(experiments[0]));
  const [baseline, setBaseline] = useState<ExperimentDefinitionV1>(() => clone(experiments[0]));
  const [jsonText, setJsonText] = useState(() => JSON.stringify(experiments[0], null, 2));
  const [errors, setErrors] = useState<string[]>([]);
  const [tab, setTab] = useState<EditorTab>("Definition");
  const [presets, setPresets] = useState<StoredPreset[]>([]);
  const [activePresetId, setActivePresetId] = useState<string | null>(null);
  const [notice, setNotice] = useState("Editing a built-in creates a custom copy when saved.");
  const importRef = useRef<HTMLInputElement>(null);
  useEffect(() => setPresets(loadPresets()), []);

  const validation = useMemo(() => experimentDefinitionSchema.safeParse(definition), [definition]);
  function update(next: ExperimentDefinitionV1) { setDefinition(next); setJsonText(JSON.stringify(next, null, 2)); setErrors([]); }
  function chooseTemplate(id: string) {
    const next = id === "blank" ? clone(blankExperiment) : clone(experiments.find((item) => item.id === id) ?? experiments[0]);
    setBaseline(next); update(next); setActivePresetId(null); setNotice("Template loaded. Save to create a custom preset.");
  }
  function choosePreset(id: string) {
    const preset = presets.find((item) => item.id === id); if (!preset) return;
    const next = clone(preset.definition); setBaseline(next); update(next); setActivePresetId(id); setNotice(`Editing preset: ${preset.name}`);
  }
  function persist(nextPresets: StoredPreset[]) { setPresets(nextPresets); savePresets(nextPresets); }
  function save() {
    const parsed = experimentDefinitionSchema.safeParse(definition);
    if (!parsed.success) { setErrors(validationMessages(parsed.error)); setTab("JSON"); return; }
    if (activePresetId) {
      const next = presets.map((preset) => preset.id === activePresetId ? { ...preset, name: parsed.data.title, definition: parsed.data, updatedAt: new Date().toISOString() } : preset);
      persist(next); setBaseline(clone(parsed.data)); setNotice("Preset saved locally.");
    } else {
      const preset = makePreset(parsed.data); persist([...presets, preset]); setActivePresetId(preset.id); setDefinition(clone(preset.definition)); setJsonText(JSON.stringify(preset.definition, null, 2)); setBaseline(clone(preset.definition)); setNotice("Custom preset created locally.");
    }
  }
  function duplicate() {
    const copy = makePreset({ ...definition, title: `${definition.title} copy` }, `${definition.title} copy`);
    persist([...presets, copy]); setActivePresetId(copy.id); setBaseline(clone(copy.definition)); update(clone(copy.definition)); setNotice("Preset duplicated.");
  }
  function removePreset() {
    if (!activePresetId) return;
    persist(presets.filter((preset) => preset.id !== activePresetId)); chooseTemplate(experiments[0].id); setNotice("Custom preset removed from this browser.");
  }
  function exportDefinition() {
    const blob = new Blob([JSON.stringify(definition, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(blob); const anchor = document.createElement("a"); anchor.href = url; anchor.download = `${definition.id}.json`; anchor.click(); URL.revokeObjectURL(url);
  }
  async function importDefinition(file: File) {
    try {
      const parsed = experimentDefinitionSchema.parse(JSON.parse(await file.text()) as unknown);
      const preset = makePreset(parsed); persist([...presets, preset]); setActivePresetId(preset.id); setBaseline(clone(preset.definition)); update(clone(preset.definition)); setNotice("Imported as a custom preset.");
    } catch (error) { setErrors(validationMessages(error)); setTab("JSON"); }
  }
  function editQuestion(index: number, next: QuestionDefinition) { update({ ...definition, questions: definition.questions.map((question, itemIndex) => itemIndex === index ? next : question) }); }
  function addQuestion(kind: QuestionDefinition["kind"]) {
    const id = uid(kind);
    const target = { type: "static" as const };
    const next: QuestionDefinition = kind === "choice" ? { id, kind, target, label: "New choice", instructions: "Choose the best supported option.", criteria: { optionA: "Evidence supports option A.", optionB: "Evidence supports option B." } }
      : kind === "score" ? { id, kind, target, label: "New score", instructions: "Select the closest ordered level.", levels: [{ label: "Low", description: "Little evidence." }, { label: "High", description: "Strong evidence." }] }
      : { id, kind, target, label: "New yes/no check", instructions: "Determine whether the proposition is supported.", criteria: { true: "Evidence supports yes.", false: "Evidence supports no." } };
    update({ ...definition, questions: [...definition.questions, next] });
  }

  return <main className="min-h-screen"><div className="mx-auto max-w-[1600px] px-5 py-6 sm:px-8 lg:px-12">
    <header className="flex flex-wrap items-center justify-between gap-4 border-b border-ink/10 pb-5"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-ink text-white"><Sparkles size={19} /></div><div><p className="text-lg font-black">Reflex Lab Studio</p><p className="text-xs font-semibold uppercase tracking-[0.2em] text-ink/45">Visual and JSON experiment authoring</p></div></div><Link href="/" className="inline-flex items-center text-sm font-bold"><ArrowLeft size={16} className="mr-2" /> Capability labs</Link></header>
    <div className="grid gap-7 py-8 lg:grid-cols-[280px_minmax(0,1fr)]">
      <aside className="space-y-5"><Card className="p-5"><p className="text-xs font-black uppercase tracking-wider text-ink/45">Start from</p><select aria-label="Built-in template" value="" onChange={(event) => chooseTemplate(event.target.value)} className="mt-3 w-full rounded-xl border border-ink/10 bg-paper p-3 text-sm font-bold"><option value="" disabled>Choose a template</option><option value="blank">Blank experiment</option>{experiments.map((item) => <option key={item.id} value={item.id}>{item.title}</option>)}</select><Button onClick={() => chooseTemplate("blank")} className="mt-3 w-full border border-ink/10"><FilePlus2 size={15} className="mr-2" /> New blank</Button></Card>
        <Card className="p-5"><div className="flex items-center justify-between"><p className="text-xs font-black uppercase tracking-wider text-ink/45">Browser presets</p><span className="rounded-full bg-paper px-2 py-1 text-xs font-black">{presets.length}</span></div><div className="mt-3 space-y-2">{presets.length === 0 ? <p className="text-sm leading-6 text-ink/45">Saved custom definitions appear here.</p> : presets.map((preset) => <button key={preset.id} onClick={() => choosePreset(preset.id)} className={cn("w-full rounded-xl p-3 text-left text-sm font-bold", activePresetId === preset.id ? "bg-ink text-white" : "bg-paper hover:bg-ink/10")}>{preset.name}</button>)}</div></Card>
        <Card className="p-5"><p className="text-xs leading-5 text-ink/50">{notice}</p><div className="mt-4 grid grid-cols-2 gap-2"><Button onClick={save} className="bg-ink text-white"><Save size={14} className="mr-2" /> Save</Button><Button onClick={duplicate} className="border border-ink/10"><Copy size={14} className="mr-2" /> Duplicate</Button><Button onClick={() => update(clone(baseline))} className="border border-ink/10"><RotateCcw size={14} className="mr-2" /> Reset</Button><Button onClick={exportDefinition} className="border border-ink/10"><Download size={14} className="mr-2" /> Export</Button><Button onClick={() => importRef.current?.click()} className="border border-ink/10"><Upload size={14} className="mr-2" /> Import</Button><input ref={importRef} type="file" accept="application/json" className="hidden" onChange={(event) => { const file = event.target.files?.[0]; if (file) void importDefinition(file); }} />{activePresetId && <Button onClick={removePreset} className="border border-coral/30 text-coral"><Trash2 size={14} className="mr-2" /> Delete</Button>}</div></Card>
      </aside>
      <section className="min-w-0"><div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><div className="flex items-center gap-2 text-xs font-black uppercase tracking-wider text-ink/45"><Wrench size={14} /> ExperimentDefinitionV1</div><h1 className="mt-2 text-4xl font-black tracking-tight">{definition.title}</h1><p className="mt-2 max-w-3xl text-ink/55">Edit visually or work directly in the synchronized JSON definition. Built-in templates are never modified.</p></div><div className={cn("rounded-full px-3 py-2 text-xs font-black", validation.success ? "bg-moss/15 text-moss" : "bg-coral/15 text-coral")}>{validation.success ? <><Check size={14} className="mr-1 inline" /> Valid schema</> : `${validation.error.issues.length} validation errors`}</div></div>
        <Card className="overflow-hidden"><div className="flex gap-1 overflow-x-auto border-b border-ink/10 px-4 pt-3">{editorTabs.map((item) => <button key={item} onClick={() => setTab(item)} className={cn("border-b-2 px-4 py-3 text-sm font-bold", tab === item ? "border-ink" : "border-transparent text-ink/40")}>{item}</button>)}</div><div className="p-5 sm:p-7">
          {tab === "Definition" && <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Title<input value={definition.title} onChange={(event) => update({ ...definition, title: event.target.value })} className="mt-2 w-full rounded-xl border border-ink/10 bg-paper p-3 font-normal" /></label><label className="text-sm font-bold">Identifier<input value={definition.id} onChange={(event) => update({ ...definition, id: event.target.value })} className="mt-2 w-full rounded-xl border border-ink/10 bg-paper p-3 font-mono text-xs font-normal" /></label><label className="text-sm font-bold sm:col-span-2">Description<textarea value={definition.description} onChange={(event) => update({ ...definition, description: event.target.value })} className="mt-2 min-h-24 w-full rounded-xl border border-ink/10 bg-paper p-3 font-normal" /></label></div><div><div className="mb-3 flex items-center justify-between"><h2 className="font-black">State fields</h2><Button onClick={() => update({ ...definition, fields: [...definition.fields, { id: uid("field"), type: "multiline", label: "New field", description: "", required: true }] })} className="border border-ink/10"><Plus size={14} className="mr-2" /> Add field</Button></div><div className="space-y-3">{definition.fields.map((field, index) => <div key={`${field.id}-${index}`} className="grid gap-3 rounded-xl bg-paper p-4 sm:grid-cols-[1fr_1fr_140px_auto]"><input aria-label={`Field ${index + 1} identifier`} value={field.id} onChange={(event) => update({ ...definition, fields: definition.fields.map((item, itemIndex) => itemIndex === index ? { ...item, id: event.target.value } : item) })} className="rounded-lg border border-ink/10 bg-white p-2 font-mono text-xs" /><input aria-label={`Field ${index + 1} label`} value={field.label} onChange={(event) => update({ ...definition, fields: definition.fields.map((item, itemIndex) => itemIndex === index ? { ...item, label: event.target.value } : item) })} className="rounded-lg border border-ink/10 bg-white p-2 text-sm" /><select aria-label={`Field ${index + 1} type`} value={field.type} onChange={(event) => { const type = event.target.value; const base = { id: field.id, label: field.label, description: field.description, required: field.required }; const next = type === "records" ? { ...base, type: "records" as const, minItems: 1, maxItems: 6, recordLabelField: "name", columns: [{ id: "name", label: "Name", multiline: false }] } : { ...base, type: type as "text" | "multiline" | "json" }; update({ ...definition, fields: definition.fields.map((item, itemIndex) => itemIndex === index ? next : item) }); }} className="rounded-lg border border-ink/10 bg-white p-2 text-sm"><option value="text">Text</option><option value="multiline">Multiline</option><option value="json">JSON</option><option value="records">Records</option></select><button aria-label={`Delete field ${index + 1}`} onClick={() => update({ ...definition, fields: definition.fields.filter((_, itemIndex) => itemIndex !== index) })}><Trash2 size={16} /></button></div>)}</div></div></div>}
          {tab === "Questions" && <div><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-black">Atomic questions</h2><p className="mt-1 text-sm text-ink/45">Static questions run once; record questions repeat over a record-list field.</p></div><div className="flex gap-2">{(["choice", "score", "noul"] as const).map((kind) => <Button key={kind} onClick={() => addQuestion(kind)} className="border border-ink/10"><Plus size={13} className="mr-1" /> {kind}</Button>)}</div></div><div className="space-y-4">{definition.questions.map((question, index) => <div key={`${question.id}-${index}`} className="rounded-2xl border border-ink/10 p-4"><div className="flex items-start justify-between gap-3"><div><span className="rounded-full bg-paper px-2 py-1 text-xs font-black uppercase">{question.kind}</span><span className="ml-2 font-mono text-xs text-ink/40">{question.id}</span></div><button aria-label={`Delete question ${question.label}`} onClick={() => update({ ...definition, questions: definition.questions.filter((_, itemIndex) => itemIndex !== index) })}><Trash2 size={16} /></button></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><input aria-label={`Question ${index + 1} label`} value={question.label} onChange={(event) => editQuestion(index, { ...question, label: event.target.value })} className="rounded-xl border border-ink/10 bg-paper p-3 text-sm font-bold" /><select aria-label={`Question ${index + 1} target`} value={question.target.type === "static" ? "static" : question.target.fieldId} onChange={(event) => editQuestion(index, { ...question, target: event.target.value === "static" ? { type: "static" } : { type: "records", fieldId: event.target.value } })} className="rounded-xl border border-ink/10 bg-paper p-3 text-sm"><option value="static">Static</option>{definition.fields.filter((field) => field.type === "records").map((field) => <option key={field.id} value={field.id}>Repeat: {field.label}</option>)}</select><textarea aria-label={`Question ${index + 1} instructions`} value={question.instructions} onChange={(event) => editQuestion(index, { ...question, instructions: event.target.value })} className="min-h-20 rounded-xl border border-ink/10 bg-paper p-3 text-sm sm:col-span-2" /></div>
                {question.kind === "choice" && <div className="mt-3 space-y-2">{Object.entries(question.criteria).map(([option, criterion]) => <div key={option} className="grid gap-2 sm:grid-cols-[150px_1fr]"><input value={option} readOnly className="rounded-lg border border-ink/10 bg-paper p-2 font-mono text-xs" /><input value={criterion} onChange={(event) => editQuestion(index, { ...question, criteria: { ...question.criteria, [option]: event.target.value } })} className="rounded-lg border border-ink/10 p-2 text-sm" /></div>)}</div>}
                {question.kind === "score" && <div className="mt-3 space-y-2">{question.levels.map((level, levelIndex) => <div key={levelIndex} className="grid gap-2 sm:grid-cols-[150px_1fr]"><input value={level.label} onChange={(event) => editQuestion(index, { ...question, levels: question.levels.map((item, itemIndex) => itemIndex === levelIndex ? { ...item, label: event.target.value } : item) })} className="rounded-lg border border-ink/10 bg-paper p-2 text-sm font-bold" /><input value={level.description} onChange={(event) => editQuestion(index, { ...question, levels: question.levels.map((item, itemIndex) => itemIndex === levelIndex ? { ...item, description: event.target.value } : item) })} className="rounded-lg border border-ink/10 p-2 text-sm" /></div>)}</div>}
                {question.kind === "noul" && <div className="mt-3 grid gap-2 sm:grid-cols-2"><textarea aria-label={`${question.label} true criteria`} value={question.criteria.true} onChange={(event) => editQuestion(index, { ...question, criteria: { ...question.criteria, true: event.target.value } })} className="min-h-20 rounded-lg border border-moss/20 bg-moss/5 p-2 text-sm" /><textarea aria-label={`${question.label} false criteria`} value={question.criteria.false} onChange={(event) => editQuestion(index, { ...question, criteria: { ...question.criteria, false: event.target.value } })} className="min-h-20 rounded-lg border border-coral/20 bg-coral/5 p-2 text-sm" /></div>}
              </div>)}</div></div>}
          {tab === "Policy" && <div className="space-y-7"><div><h2 className="font-black">Weighted metrics</h2><p className="mt-1 text-sm text-ink/45">Weights and inversions are deterministic client-side composition.</p><div className="mt-4 space-y-3">{definition.policy.metrics.map((metric, index) => <div key={metric.id} className="grid gap-3 rounded-xl bg-paper p-4 sm:grid-cols-[1fr_180px_100px_auto]"><input value={metric.label} onChange={(event) => update({ ...definition, policy: { ...definition.policy, metrics: definition.policy.metrics.map((item, itemIndex) => itemIndex === index ? { ...item, label: event.target.value } : item) } })} className="rounded-lg border border-ink/10 bg-white p-2 text-sm font-bold" /><select value={metric.source.questionId} onChange={(event) => update({ ...definition, policy: { ...definition.policy, metrics: definition.policy.metrics.map((item, itemIndex) => itemIndex === index ? { ...item, source: { ...item.source, questionId: event.target.value } } : item) } })} className="rounded-lg border border-ink/10 bg-white p-2 text-sm">{definition.questions.map((question) => <option key={question.id} value={question.id}>{question.label}</option>)}</select><input aria-label={`${metric.label} weight`} type="number" min="0" max="10" step="0.1" value={metric.weight} onChange={(event) => update({ ...definition, policy: { ...definition.policy, metrics: definition.policy.metrics.map((item, itemIndex) => itemIndex === index ? { ...item, weight: Number(event.target.value) } : item) } })} className="rounded-lg border border-ink/10 bg-white p-2 text-sm" /><button aria-label={`Delete metric ${metric.label}`} onClick={() => update({ ...definition, policy: { ...definition.policy, metrics: definition.policy.metrics.filter((_, itemIndex) => itemIndex !== index) } })}><Trash2 size={16} /></button></div>)}</div></div><div><h2 className="font-black">Ordered outcome rules</h2><div className="mt-4 space-y-3">{definition.policy.rules.map((rule, index) => <div key={rule.id} className="rounded-xl border border-ink/10 p-4"><div className="grid gap-3 sm:grid-cols-[1fr_130px]"><input value={rule.label} onChange={(event) => update({ ...definition, policy: { ...definition.policy, rules: definition.policy.rules.map((item, itemIndex) => itemIndex === index ? { ...item, label: event.target.value } : item) } })} className="rounded-lg border border-ink/10 bg-paper p-2 font-bold" /><select value={rule.outcome} onChange={(event) => update({ ...definition, policy: { ...definition.policy, rules: definition.policy.rules.map((item, itemIndex) => itemIndex === index ? { ...item, outcome: event.target.value as "act" | "review" | "stop" } : item) } })} className="rounded-lg border border-ink/10 bg-paper p-2"><option value="act">Act</option><option value="review">Review</option><option value="stop">Stop</option></select></div><textarea value={rule.recommendation} onChange={(event) => update({ ...definition, policy: { ...definition.policy, rules: definition.policy.rules.map((item, itemIndex) => itemIndex === index ? { ...item, recommendation: event.target.value } : item) } })} className="mt-3 min-h-16 w-full rounded-lg border border-ink/10 bg-paper p-2 text-sm" />{rule.all.map((condition, conditionIndex) => <div key={conditionIndex} className="mt-2 flex items-center gap-3 rounded-lg bg-paper p-3 text-xs"><span className="flex-1 font-mono">{condition.type === "metric" ? condition.metricId : condition.questionId} · {condition.operator}</span>{condition.type === "metric" ? <input aria-label={`${rule.label} threshold ${conditionIndex + 1}`} type="number" min="0" max="1" step="0.05" value={condition.value} onChange={(event) => update({ ...definition, policy: { ...definition.policy, rules: definition.policy.rules.map((item, itemIndex) => itemIndex === index ? { ...item, all: item.all.map((entry, entryIndex) => entryIndex === conditionIndex && entry.type === "metric" ? { ...entry, value: Number(event.target.value) } : entry) } : item) } })} className="w-20 rounded border border-ink/10 bg-white p-1" /> : <span>{condition.value}</span>}</div>)}</div>)}</div></div></div>}
          {tab === "JSON" && <div><div className="mb-3 flex items-center gap-2"><Braces size={17} /><h2 className="font-black">Full definition JSON</h2></div><textarea aria-label="Experiment definition JSON" value={jsonText} onChange={(event) => { const text = event.target.value; setJsonText(text); try { const parsed = experimentDefinitionSchema.parse(JSON.parse(text) as unknown); setDefinition(parsed); setErrors([]); } catch (error) { setErrors(validationMessages(error)); } }} spellCheck={false} className="min-h-[650px] w-full rounded-2xl bg-[#111923] p-5 font-mono text-xs leading-5 text-white outline-none" />{errors.length > 0 && <div role="alert" className="mt-4 rounded-xl border border-coral/30 bg-coral/10 p-4"><p className="font-black text-coral">Definition errors</p><ul className="mt-2 space-y-1 font-mono text-xs text-[#8f3c2b]">{errors.map((error, index) => <li key={index}>{error}</li>)}</ul></div>}</div>}
        </div></Card>
      </section>
    </div>
  </div></main>;
}
